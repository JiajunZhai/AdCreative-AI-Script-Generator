[
  {
    "search": "            </div>\n          </div>\n        )}",
    "replace": "            </div>\n          </div>\n        )}\n\n        {!!globalDefaultId && (\n          <div className=\"mb-4 rounded-xl border border-outline-variant/40 bg-surface-container-lowest px-4 py-3 shadow-sm\">\n            <div className=\"flex items-center justify-between gap-3 mb-2\">\n              <div className=\"flex flex-col\">\n                <span className=\"text-[11px] font-bold text-on-surface-variant uppercase tracking-wider\">\n                  备用降级顺序 (Failover Order)\n                </span>\n                <span className=\"text-[10px] text-on-surface-variant/70\">\n                  当当前首选引擎不可用时，系统将按此顺序自动切换(multi-hop retry)\n                </span>\n              </div>\n              <button\n                type=\"button\"\n                onClick={() => {\n                  if (showFallbackEdit) {\n                    handleSaveFallbackOrder();\n                  } else {\n                    setFallbackDraft(fallbackProviders);\n                    setShowFallbackEdit(true);\n                  }\n                }}\n                className=\"inline-flex items-center gap-1.5 text-[11px] font-bold px-2 py-1 rounded-lg border text-primary border-primary/30 hover:bg-primary/10 transition-colors\"\n              >\n                {showFallbackEdit ? <Save className=\"w-3 h-3\" /> : <Plug className=\"w-3 h-3\" />}\n                {showFallbackEdit ? t('providers.save') : t('providers.edit')}\n              </button>\n            </div>\n\n            {showFallbackEdit ? (\n              <div className=\"flex flex-col gap-1\">\n                {catalog\n                  .filter(p => p.id !== globalDefaultId)\n                  .map(p => {\n                    const isSelected = fallbackDraft.includes(p.id);\n                    return (\n                      <label key={p.id} className=\"flex items-center gap-2 text-[12px] cursor-pointer hover:bg-outline-variant/10 px-2 py-1 rounded-md\">\n                        <input\n                          type=\"checkbox\"\n                          checked={isSelected}\n                          onChange={() => {\n                            if (isSelected) {\n                              setFallbackDraft(prev => prev.filter(id => id !== p.id));\n                            } else {\n                              setFallbackDraft(prev => [...prev, p.id]);\n                            }\n                          }}\n                          className=\"rounded border-outline-variant text-primary focus:ring-primary\"\n                        />\n                        <span className=\"font-semibold text-on-surface\">{p.label}</span>\n                        {isSelected ? <span className=\"text-primary ml-auto font-bold\">#{fallbackDraft.indexOf(p.id) + 1}</span> : null}\n                      </label>\n                    );\n                })}\n              </div>\n            ) : (\n              <div className=\"flex flex-wrap gap-2\">\n                {fallbackProviders.length === 0 ? (\n                  <span className=\"text-[11px] text-on-surface-variant/50\">None. Requests will fail if the primary engine is unavailable.</span>\n                ) : (\n                  fallbackProviders.map((id, index) => (\n                    <div key={id} className=\"flex items-center gap-1 text-[11px] font-semibold px-2 py-0.5 bg-surface-container-high border border-outline-variant/10 rounded-full\">\n                      <span className=\"text-primary opacity-60\">#{index + 1}</span>\n                      <span>{catalog.find(p => p.id === id)?.label || id}</span>\n                    </div>\n                  ))\n                )}\n              </div>\n            )}\n          </div>\n        )}"
  },
  {
    "search": "mait refresh();",
    "replace": "await refresh();"
  }
]
